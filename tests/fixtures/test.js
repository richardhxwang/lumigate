// Test JavaScript file for LumiChat upload
const greet = (name) => `Hello, ${name}!`;

function factorial(n) {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
}

module.exports = { greet, factorial };
